# 🚀 Руководство по развертыванию интеграции Яндекс.Маркет ↔ МойСклад

## ⚠️ Важно!

**Локальное тестирование через ngrok/localtunnel НЕВОЗМОЖНО!**

Яндекс.Маркет требует:
- ✅ HTTPS с официальным SSL-сертификатом (от Let's Encrypt, Comodo и т.д.)
- ❌ Самоподписанные сертификаты НЕ подходят
- ❌ Туннели (ngrok, localtunnel) НЕ подходят

**Для работы webhook обязательно нужен реальный сервер с доменом и SSL.**

---

## 📋 Что уже готово

✅ Маппинг товаров создан (780 товаров)
✅ Webhook сервер написан и протестирован
✅ Идемпотентность реализована
✅ Логирование настроено
✅ Обработка ORDER_CREATED и ORDER_STATUS_UPDATED

---

## 🎯 Варианты развертывания

### Вариант 1: VPS с ручной настройкой (Рекомендуется)

**Преимущества:**
- Полный контроль
- Низкая стоимость (от 200₽/месяц)
- Можно использовать для других проектов

**Провайдеры:**
- **Timeweb** - от 200₽/месяц (Россия)
- **Hetzner** - от €4/месяц (Европа)
- **DigitalOcean** - от $6/месяц (США)

**Что нужно сделать:**

1. **Арендовать VPS**
   - Минимум: 1 CPU, 1GB RAM, 10GB SSD
   - ОС: Ubuntu 22.04 LTS

2. **Зарегистрировать домен**
   - Купить домен (например, на reg.ru, timeweb.com)
   - Или использовать существующий
   - Настроить A-запись на IP сервера

3. **Настроить сервер**
   ```bash
   # Подключиться к серверу
   ssh root@your-server-ip
   
   # Обновить систему
   apt update && apt upgrade -y
   
   # Установить Node.js
   curl -fsSL https://deb.nodesource.com/setup_20.x | bash -
   apt install -y nodejs
   
   # Установить PM2 (менеджер процессов)
   npm install -g pm2
   
   # Установить Nginx
   apt install -y nginx
   
   # Установить Certbot (для SSL)
   apt install -y certbot python3-certbot-nginx
   ```

4. **Загрузить код на сервер**
   ```bash
   # На локальной машине
   scp -r ./* root@your-server-ip:/var/www/yandex-moysklad/
   
   # Или через Git
   ssh root@your-server-ip
   cd /var/www
   git clone your-repo-url yandex-moysklad
   cd yandex-moysklad
   npm install
   ```

5. **Настроить .env на сервере**
   ```bash
   nano /var/www/yandex-moysklad/.env
   # Скопировать содержимое из локального .env
   ```

6. **Настроить Nginx**
   ```bash
   nano /etc/nginx/sites-available/yandex-moysklad
   ```
   
   Содержимое файла:
   ```nginx
   server {
       listen 80;
       server_name your-domain.com;
       
       location / {
           proxy_pass http://localhost:3000;
           proxy_http_version 1.1;
           proxy_set_header Upgrade $http_upgrade;
           proxy_set_header Connection 'upgrade';
           proxy_set_header Host $host;
           proxy_cache_bypass $http_upgrade;
           proxy_set_header X-Real-IP $remote_addr;
           proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
           proxy_set_header X-Forwarded-Proto $scheme;
       }
   }
   ```
   
   Активировать конфигурацию:
   ```bash
   ln -s /etc/nginx/sites-available/yandex-moysklad /etc/nginx/sites-enabled/
   nginx -t
   systemctl restart nginx
   ```

7. **Получить SSL-сертификат**
   ```bash
   certbot --nginx -d your-domain.com
   # Следовать инструкциям, ввести email
   # Certbot автоматически настроит HTTPS
   ```

8. **Запустить приложение через PM2**
   ```bash
   cd /var/www/yandex-moysklad
   pm2 start index.js --name yandex-moysklad
   pm2 save
   pm2 startup
   ```

9. **Настроить webhook в Яндекс.Маркет**
   - Зайти в личный кабинет партнера
   - Настройки → API-уведомления
   - URL: `https://your-domain.com/webhook`
   - Выбрать события: ORDER_CREATED, ORDER_STATUS_UPDATED
   - Нажать "Проверить URL" → "Сохранить"

---

### Вариант 2: Railway.app (Самый простой)

**Преимущества:**
- Автоматическое развертывание из Git
- Автоматический SSL
- Бесплатный план (с ограничениями)

**Что нужно сделать:**

1. **Зарегистрироваться на railway.app**

2. **Создать новый проект**
   - New Project → Deploy from GitHub repo
   - Выбрать ваш репозиторий

3. **Настроить переменные окружения**
   - Settings → Variables
   - Добавить все переменные из .env

4. **Получить URL**
   - Railway автоматически создаст URL вида: `your-app.railway.app`
   - Или подключить свой домен

5. **Настроить webhook в Яндекс.Маркет**
   - URL: `https://your-app.railway.app/webhook`

---

### Вариант 3: Heroku

**Преимущества:**
- Простое развертывание
- Автоматический SSL
- Хорошая документация

**Что нужно сделать:**

1. **Установить Heroku CLI**
   ```bash
   # macOS
   brew tap heroku/brew && brew install heroku
   ```

2. **Создать приложение**
   ```bash
   heroku login
   heroku create your-app-name
   ```

3. **Настроить переменные окружения**
   ```bash
   heroku config:set MOYSKLAD_BASE=https://api.moysklad.ru/api/remap/1.2
   heroku config:set MOYSKLAD_PASSWORD=your-token
   # ... остальные переменные
   ```

4. **Развернуть**
   ```bash
   git push heroku main
   ```

5. **Получить URL**
   - `https://your-app-name.herokuapp.com`

6. **Настроить webhook в Яндекс.Маркет**
   - URL: `https://your-app-name.herokuapp.com/webhook`

---

## 🔧 После развертывания

### 1. Проверить работу сервера

```bash
# Проверить, что сервер отвечает
curl https://your-domain.com/

# Должен вернуть: "Yandex.Market to MoySklad Integration Server"
```

### 2. Проверить логи

**VPS:**
```bash
pm2 logs yandex-moysklad
```

**Railway/Heroku:**
- Смотреть логи в веб-интерфейсе

### 3. Создать тестовый заказ

1. Создать заказ в Яндекс.Маркет
2. Проверить логи сервера
3. Проверить МойСклад → Продажи → Заказы покупателей

### 4. Проверить создание отгрузки

1. Изменить статус заказа на READY_TO_SHIP
2. Проверить логи сервера
3. Проверить МойСклад → Продажи → Отгрузки

---

## 📊 Мониторинг

### Логи

**Что логируется:**
- Получение webhook от Яндекса
- Создание заказов в МойСклад
- Создание отгрузок в МойСклад
- Ошибки и предупреждения

**Формат логов:**
```
[WEBHOOK] Received notification: type="ORDER_CREATED", orderId="12345"
[ORDER_CREATED] Processing order: orderId="YM-12345"
[MAPPING] Mapped: SKU="..." -> ProductID="...", qty=1
[ORDER_CREATED] Success: orderId="YM-12345"
```

### Проверка здоровья

Создать простой скрипт для проверки:
```bash
#!/bin/bash
# health-check.sh

RESPONSE=$(curl -s -o /dev/null -w "%{http_code}" https://your-domain.com/)

if [ $RESPONSE -eq 200 ]; then
    echo "✅ Server is healthy"
else
    echo "❌ Server is down (HTTP $RESPONSE)"
    # Отправить уведомление (email, Telegram и т.д.)
fi
```

Добавить в cron (каждые 5 минут):
```bash
crontab -e
# Добавить строку:
*/5 * * * * /path/to/health-check.sh
```

---

## 🆘 Troubleshooting

### Проблема: Webhook не приходят

**Решение:**
1. Проверить, что сервер доступен: `curl https://your-domain.com/`
2. Проверить SSL: `curl -v https://your-domain.com/`
3. Проверить настройки webhook в Яндекс.Маркет
4. Проверить логи сервера

### Проблема: Заказы не создаются в МойСклад

**Решение:**
1. Проверить логи - есть ли ошибки?
2. Проверить mapping.json - есть ли SKU товара?
3. Проверить .env - правильные ли данные МойСклад?
4. Проверить токен МойСклад - не истек ли?

### Проблема: Unmapped SKU

**Решение:**
1. Товар не найден в mapping.json
2. Запустить `npm start` на сервере чтобы обновить маппинг
3. Проверить, что в МойСклад у товара заполнен артикул

---

## 🔄 Обновление кода

**VPS:**
```bash
ssh root@your-server-ip
cd /var/www/yandex-moysklad
git pull
npm install
pm2 restart yandex-moysklad
```

**Railway/Heroku:**
- Просто сделать `git push` - развертывание автоматическое

---

## 💰 Примерная стоимость

| Вариант | Стоимость | Сложность |
|---------|-----------|-----------|
| VPS (Timeweb) | 200₽/мес | Средняя |
| VPS (Hetzner) | €4/мес (~400₽) | Средняя |
| Railway.app | $5/мес (бесплатно с ограничениями) | Низкая |
| Heroku | $7/мес | Низкая |

**Рекомендация:** Начать с Railway.app для быстрого старта, потом перейти на VPS для экономии.

---

## ✅ Чеклист готовности к продакшн

- [ ] Сервер арендован
- [ ] Домен зарегистрирован и настроен
- [ ] SSL-сертификат установлен
- [ ] Код развернут на сервере
- [ ] .env настроен с правильными данными
- [ ] mapping.json создан (780 товаров)
- [ ] Сервер запущен и отвечает на запросы
- [ ] Webhook настроен в Яндекс.Маркет
- [ ] Тестовый заказ успешно обработан
- [ ] Мониторинг настроен
- [ ] Логи проверяются регулярно

---

## 📞 Поддержка

Если возникли проблемы:
1. Проверить логи сервера
2. Проверить файл .env
3. Проверить mapping.json
4. Написать в поддержку Яндекс.Маркет или МойСклад

Удачи с развертыванием! 🚀
