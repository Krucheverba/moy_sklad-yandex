# 🚀 Пошаговая инструкция: Настройка сервера на RUVDS

## Шаг 1: Аренда VPS на RUVDS

1. **Зайти на сайт:** https://ruvds.com/
2. **Нажать "Зарегистрироваться"** (зеленая кнопка справа вверху)
3. **Заполнить форму регистрации:**
   - Email
   - Пароль
   - Подтвердить email

4. **Выбрать тариф "Старт Хит":**
   - Перейти в раздел "VPS" → "Тарифы Старт"
   - Выбрать "Старт Хит" (259₽/мес)
   - 1 CPU, 1GB RAM, 20GB HDD

5. **Настроить параметры:**
   - **ОС:** Linux → Ubuntu 22.04 LTS
   - **Локация:** Москва (ближе к Яндексу)
   - **Период:** 1 месяц (для начала)

6. **Оплатить:**
   - Пополнить баланс на 259₽
   - Подтвердить заказ

7. **Получить данные доступа:**
   - После создания VPS придет письмо на email
   - В письме будет:
     - IP адрес сервера
     - Логин: `root`
     - Пароль

---

## Шаг 2: Подключение к серверу

### На macOS (у тебя):

1. **Открыть Terminal** (Cmd + Space → "Terminal")

2. **Подключиться к серверу:**
   ```bash
   ssh root@YOUR_SERVER_IP
   ```
   
   Замени `YOUR_SERVER_IP` на IP из письма, например:
   ```bash
   ssh root@185.123.45.67
   ```

3. **При первом подключении:**
   - Появится вопрос: `Are you sure you want to continue connecting?`
   - Напиши `yes` и нажми Enter

4. **Ввести пароль:**
   - Скопируй пароль из письма
   - Вставь в терминал (Cmd + V)
   - Пароль не будет виден при вводе - это нормально!
   - Нажми Enter

5. **Ты подключен!**
   - Увидишь приглашение вида: `root@server:~#`
   - Теперь у тебя полный доступ к серверу

---

## Шаг 3: Первоначальная настройка сервера

### 3.1. Обновить систему

```bash
# Обновить список пакетов
apt update

# Установить обновления
apt upgrade -y
```

Это займет 2-5 минут. Дождись завершения.

### 3.2. Установить Node.js 20

```bash
# Добавить репозиторий Node.js
curl -fsSL https://deb.nodesource.com/setup_20.x | bash -

# Установить Node.js
apt install -y nodejs

# Проверить установку
node --version
# Должно показать: v20.x.x

npm --version
# Должно показать: 10.x.x
```

### 3.3. Установить PM2 (менеджер процессов)

```bash
# Установить PM2 глобально
npm install -g pm2

# Проверить установку
pm2 --version
```

### 3.4. Установить Nginx

```bash
# Установить Nginx
apt install -y nginx

# Проверить статус
systemctl status nginx
# Должно быть: active (running)

# Если не запущен, запустить:
systemctl start nginx
systemctl enable nginx
```

### 3.5. Установить Certbot (для SSL)

```bash
# Установить Certbot
apt install -y certbot python3-certbot-nginx

# Проверить установку
certbot --version
```

### 3.6. Настроить firewall (опционально, но рекомендуется)

```bash
# Установить UFW
apt install -y ufw

# Разрешить SSH (важно!)
ufw allow 22/tcp

# Разрешить HTTP и HTTPS
ufw allow 80/tcp
ufw allow 443/tcp

# Включить firewall
ufw enable

# Проверить статус
ufw status
```

---

## Шаг 4: Загрузка кода на сервер

### Вариант 1: Через SCP (простой способ)

**На твоем локальном Mac:**

```bash
# Перейти в папку с проектом
cd /path/to/yandex-moysklad

# Создать архив (исключая node_modules)
tar -czf project.tar.gz --exclude='node_modules' --exclude='.git' .

# Загрузить на сервер
scp project.tar.gz root@YOUR_SERVER_IP:/root/
```

**На сервере:**

```bash
# Создать папку для проекта
mkdir -p /var/www/yandex-moysklad

# Распаковать архив
tar -xzf /root/project.tar.gz -C /var/www/yandex-moysklad

# Перейти в папку
cd /var/www/yandex-moysklad

# Установить зависимости
npm install
```

### Вариант 2: Через Git (если используешь Git)

**На сервере:**

```bash
# Установить Git
apt install -y git

# Клонировать репозиторий
cd /var/www
git clone YOUR_REPO_URL yandex-moysklad

# Перейти в папку
cd yandex-moysklad

# Установить зависимости
npm install
```

---

## Шаг 5: Настройка .env на сервере

```bash
# Перейти в папку проекта
cd /var/www/yandex-moysklad

# Создать .env файл
nano .env
```

**Скопируй содержимое из локального .env:**

```env
PORT=3000
MOYSKLAD_BASE=https://api.moysklad.ru/api/remap/1.2
MOYSKLAD_LOGIN=admin@vadimvvb011
MOYSKLAD_PASSWORD=12272809448aaf9be5863c1ff5b9e82071afcb9b
YANDEX_TOKEN=y0__xD7rP4QGLnnOyDcwMmqFTCgt7WvCFs2LDY15Oscyxl-kl6RkB3o58QH
YANDEX_CAMPAIGN_ID=128441417
STORE_ID=eccf4c27-bebe-11ef-0a80-1181000ad2be
ORG_ID=eccb80ed-bebe-11ef-0a80-1181000ad2b8
DEFAULT_AGENT_ID=b9d0d933-bf0b-11ef-0a80-1ab00013ba5a
YANDEX_BUSINESS_ID=150922395
```

**Сохранить:**
- Нажми `Ctrl + X`
- Нажми `Y`
- Нажми `Enter`

---

## Шаг 6: Настройка Nginx

### 6.1. Создать конфигурацию

```bash
nano /etc/nginx/sites-available/yandex-moysklad
```

**Вставь этот конфиг:**

```nginx
server {
    listen 80;
    server_name YOUR_DOMAIN.com;
    
    location / {
        proxy_pass http://localhost:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
```

**Замени `YOUR_DOMAIN.com` на свой домен!**

**Сохранить:** `Ctrl + X` → `Y` → `Enter`

### 6.2. Активировать конфигурацию

```bash
# Создать символическую ссылку
ln -s /etc/nginx/sites-available/yandex-moysklad /etc/nginx/sites-enabled/

# Проверить конфигурацию
nginx -t

# Если всё ОК, перезапустить Nginx
systemctl restart nginx
```

---

## Шаг 7: Настройка домена

### 7.1. Купить домен (если еще нет)

Можно купить на:
- **Reg.ru** - от 99₽/год (.ru домен)
- **Timeweb** - от 199₽/год
- **Namecheap** - от $8/год (.com домен)

### 7.2. Настроить DNS

В панели управления доменом добавь A-запись:

```
Тип: A
Имя: @ (или оставь пустым)
Значение: YOUR_SERVER_IP
TTL: 3600
```

**Пример:**
```
A  @  185.123.45.67
```

**Ожидание:** DNS обновится через 5-30 минут.

### 7.3. Проверить домен

```bash
# На локальном Mac
ping your-domain.com

# Должен показать IP твоего сервера
```

---

## Шаг 8: Получить SSL-сертификат

**После того как домен заработал:**

```bash
# Получить сертификат
certbot --nginx -d your-domain.com

# Certbot спросит:
# 1. Email - введи свой email
# 2. Согласие с ToS - нажми Y
# 3. Рассылка - можешь нажать N
# 4. Redirect HTTP to HTTPS - нажми 2 (да, редиректить)

# Certbot автоматически:
# - Получит сертификат
# - Настроит Nginx для HTTPS
# - Настроит автообновление
```

**Проверить:**
```bash
# Открыть в браузере
https://your-domain.com

# Должен показать: "Yandex.Market to MoySklad Integration Server"
# И замочек SSL в адресной строке
```

---

## Шаг 9: Запуск приложения

### 9.1. Запустить через PM2

```bash
# Перейти в папку проекта
cd /var/www/yandex-moysklad

# Запустить приложение
pm2 start index.js --name yandex-moysklad

# Проверить статус
pm2 status

# Должно быть: status: online
```

### 9.2. Настроить автозапуск

```bash
# Сохранить список процессов
pm2 save

# Настроить автозапуск при перезагрузке
pm2 startup

# PM2 выдаст команду, скопируй и выполни её
# Например:
# sudo env PATH=$PATH:/usr/bin pm2 startup systemd -u root --hp /root
```

### 9.3. Проверить логи

```bash
# Посмотреть логи
pm2 logs yandex-moysklad

# Должно быть:
# [CONFIG] Configuration loaded successfully
# [MAPPING] Product mapping loaded: 780 SKU mappings
# Server listening on 3000
```

---

## Шаг 10: Настройка webhook в Яндекс.Маркет

1. **Зайти в личный кабинет партнера:** https://partner.market.yandex.ru

2. **Выбрать магазин** (ID: 128441417)

3. **Найти раздел:** Настройки → API-уведомления

4. **Ввести URL:**
   ```
   https://your-domain.com/webhook
   ```

5. **Выбрать события:**
   - ✅ ORDER_CREATED
   - ✅ ORDER_STATUS_UPDATED

6. **Нажать "Проверить URL"**
   - Яндекс отправит PING запрос
   - Должен вернуть 200 OK

7. **Нажать "Сохранить"**

---

## Шаг 11: Тестирование

### 11.1. Проверить сервер

```bash
# На локальном Mac
curl https://your-domain.com/

# Должен вернуть:
# Yandex.Market to MoySklad Integration Server
```

### 11.2. Создать тестовый заказ

1. Создай заказ в Яндекс.Маркет
2. Проверь логи на сервере:
   ```bash
   pm2 logs yandex-moysklad
   ```
3. Проверь МойСклад → Продажи → Заказы покупателей

### 11.3. Проверить отгрузку

1. Измени статус заказа на READY_TO_SHIP
2. Проверь логи
3. Проверь МойСклад → Продажи → Отгрузки

---

## 🔧 Полезные команды

### Управление PM2

```bash
# Посмотреть статус
pm2 status

# Посмотреть логи
pm2 logs yandex-moysklad

# Перезапустить
pm2 restart yandex-moysklad

# Остановить
pm2 stop yandex-moysklad

# Удалить из PM2
pm2 delete yandex-moysklad
```

### Управление Nginx

```bash
# Проверить конфигурацию
nginx -t

# Перезапустить
systemctl restart nginx

# Посмотреть статус
systemctl status nginx

# Посмотреть логи
tail -f /var/log/nginx/error.log
```

### Обновление кода

```bash
# Подключиться к серверу
ssh root@YOUR_SERVER_IP

# Перейти в папку
cd /var/www/yandex-moysklad

# Если через Git:
git pull

# Установить зависимости
npm install

# Перезапустить приложение
pm2 restart yandex-moysklad
```

### Обновление маппинга

```bash
# Подключиться к серверу
ssh root@YOUR_SERVER_IP

# Перейти в папку
cd /var/www/yandex-moysklad

# Запустить генерацию маппинга
npm start

# Перезапустить приложение
pm2 restart yandex-moysklad
```

---

## ❓ FAQ

### Забыл пароль от сервера?

В панели RUVDS можно:
- Сбросить пароль
- Подключиться через VNC консоль

### Сервер не отвечает?

```bash
# Проверить статус приложения
pm2 status

# Проверить статус Nginx
systemctl status nginx

# Посмотреть логи
pm2 logs
tail -f /var/log/nginx/error.log
```

### Нужно больше RAM?

В панели RUVDS можно:
- Апгрейдить тариф
- Добавить RAM без переустановки

### SSL сертификат истек?

```bash
# Обновить сертификат
certbot renew

# Certbot автоматически обновляет сертификаты
# Но можно запустить вручную
```

---

## 📞 Поддержка

**RUVDS:**
- Сайт: ruvds.com
- Email: support@ruvds.com
- Чат: есть в личном кабинете

**Если что-то не получается:**
1. Проверь логи: `pm2 logs`
2. Проверь Nginx: `nginx -t`
3. Проверь firewall: `ufw status`
4. Напиши в поддержку RUVDS

---

## ✅ Чеклист готовности

- [ ] VPS арендован на RUVDS
- [ ] Подключился по SSH
- [ ] Обновил систему
- [ ] Установил Node.js
- [ ] Установил PM2
- [ ] Установил Nginx
- [ ] Установил Certbot
- [ ] Загрузил код на сервер
- [ ] Настроил .env
- [ ] Настроил Nginx
- [ ] Купил и настроил домен
- [ ] Получил SSL-сертификат
- [ ] Запустил приложение через PM2
- [ ] Настроил webhook в Яндекс.Маркет
- [ ] Протестировал с реальным заказом

---

## 🎉 Готово!

Теперь интеграция работает автоматически 24/7!

Удачи! 🚀
